@model Nodus.Elluris.Domain.Models.EventoPeriodo

@{
    ViewData["Title"] = "Adicionar Período";
}

<div class="card">
    <div class="card-header text-white bg-info mb-3">
        <h1>Adicionar Período</h1>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <form asp-controller="EventoPeriodos" asp-action="Create">
                    <partial name="_edit_create_periodo" />
                    <div class="form-group">
                        <input type="submit" value="Salvar" class="btn btn-primary" />
                        <a asp-controller="EventoPeriodos" asp-action="Index" class="btn btn-info">Voltar à lista</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
